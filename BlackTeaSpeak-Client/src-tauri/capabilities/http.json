{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "http",
  "description": "Capability for HTTP fetch",
  "windows": ["main"],
  "permissions": [
    "http:default",
    {
      "identifier": "http:default",
      "allow": [
        { "url": "https://127.0.0.1:*" }
      ]
    }
  ]
}
